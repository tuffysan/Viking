let state={view:'dashboard',packages:[],accounts:[],orders:[],settings:null,selected:null};
const $=s=>document.querySelector(s), esc=s=>(s??'').toString().replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
async function api(url,opt){let r=await fetch(url,opt);if(!r.ok){let text=await r.text();try{let j=JSON.parse(text);throw new Error(j.error||text||r.statusText)}catch(e){if(e instanceof SyntaxError)throw new Error(text||r.statusText);throw e}}return r.status===204?null:r.json()}
function toast(m){let t=$('#toast');t.textContent=m;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2800)}
function copyText(v,label='Kopierat'){navigator.clipboard?.writeText(v).then(()=>toast(label)).catch(()=>toast('Kunde inte kopiera'))}
async function refreshAll(){try{[state.packages,state.accounts,state.orders,state.settings]=await Promise.all([api('/api/packages'),api('/api/accounts'),api('/api/orders'),api('/api/settings')]);let h=await api('/api/health');$('#healthDot').style.background='#47c98a';$('#healthText').textContent=`${h.hostname} · v${h.version}`;render()}catch(e){$('#healthDot').style.background='#ff6b6b';$('#healthText').textContent='API offline';toast(e.message||'Kunde inte uppdatera')}}
document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active');state.view=b.dataset.view;render()});
function setTitle(t,s){$('#title').textContent=t;$('#subtitle').textContent=s}
function render(){({dashboard:renderDashboard,new:renderNew,renew:renderRenew,accounts:renderAccounts,orders:renderOrders,settings:renderSettings}[state.view]||renderDashboard)()}
function money(v){return `${Number(v||0).toLocaleString('sv-SE')} kr`}
function feeConfigured(){return !!state.settings?.configured}
function feeNotice(){return feeConfigured()?`<div class="fee-box"><div><span>Obligatorisk serviceavgift</span><b>${money(state.settings.serviceFeeSek)}</b></div><small>Betalas separat till ${esc(state.settings.swishRecipientName||'administratören')} via Swish ${esc(state.settings.swishNumber)}.</small></div>`:`<div class="alert"><b>Serviceavgift saknas.</b> Ange serviceavgift och ditt Swish-nummer under Inställningar innan beställningar kan skapas.</div>`}
function renderDashboard(){setTitle('Översikt','Hantera abonnemang, förlängningar och betalningsflöde.');let active=state.accounts.filter(a=>!a.expiresOn||new Date(a.expiresOn)>=new Date()).length;$('#content').innerHTML=`<div class="grid"><div class="card metric"><span>Konton</span><b>${state.accounts.length}</b></div><div class="card metric"><span>Aktiva / okända</span><b>${active}</b></div><div class="card metric"><span>Serviceavgift</span><b>${feeConfigured()?money(state.settings.serviceFeeSek):'Ej satt'}</b></div><div class="card wide">${feeNotice()}<h2>Snabbstart</h2><div class="two"><div><p>Skapa nytt abonnemang och fortsätt till Vikings officiella beställningssida.</p><div class="actions"><button class="primary" onclick="go('new')">Nytt abonnemang</button></div></div><div><p>Förläng ett sparat konto utan att skapa ett nytt abonnemang.</p><div class="actions"><button class="primary" onclick="go('renew')">Förläng konto</button></div></div></div></div><div class="card wide"><div class="alert"><b>Betalningar hålls separata:</b> Vikings/Safellos belopp betalas till leverantörsflödet. Din obligatoriska serviceavgift betalas separat till Swish-numret som du anger under Inställningar.</div></div></div>`}
function go(v){state.view=v;document.querySelectorAll('nav button').forEach(x=>x.classList.toggle('active',x.dataset.view===v));render()}
function packageCards(){return `<div class="packages">${state.packages.map(p=>`<div class="package ${state.selected===p.id?'selected':''}" onclick="state.selected='${p.id}';render()"><h3>${esc(p.name)}</h3><div class="price">${money(p.priceSek)}</div><div class="muted">${p.connections} anslutning${p.connections>1?'ar':''}</div></div>`).join('')}</div>`}
function renderNew(){setTitle('Nytt abonnemang','Förbered en ny beställning och betalning.');if(!state.selected)state.selected=state.packages[3]?.id;$('#content').innerHTML=`<div class="card"><h2>1. Välj paket</h2>${packageCards()}</div><div class="card" style="margin-top:16px"><h2>2. Serviceavgift</h2>${feeNotice()}</div><div class="card" style="margin-top:16px"><h2>3. Leverantörsbetalning</h2><label>Betalningsmetod</label><select id="pay"><option value="safello-swish">Swish via Safello</option><option value="provider">Leverantörens övriga betalningsalternativ</option></select><label>Anteckning</label><textarea id="orderNotes" placeholder="Valfritt"></textarea><div class="actions"><button class="primary" ${feeConfigured()?'':'disabled'} onclick="createOrder('new')">Förbered beställning</button></div></div>`}
function renderRenew(){setTitle('Förläng befintligt konto','Välj sparat konto och lägg till önskad abonnemangsperiod.');if(!state.selected)state.selected=state.packages[3]?.id;$('#content').innerHTML=`<div class="card"><h2>1. Konto</h2>${state.accounts.length?`<select id="accountId">${state.accounts.map(a=>`<option value="${a.id}">${esc(a.name||a.email||'Konto')} ${a.expiresOn?'· går ut '+a.expiresOn:''}</option>`).join('')}</select>`:`<div class="alert">Inga konton finns ännu. Lägg till ditt nuvarande konto under <b>Konton</b> först.</div>`}</div><div class="card" style="margin-top:16px"><h2>2. Förlängning</h2>${packageCards()}</div><div class="card" style="margin-top:16px"><h2>3. Serviceavgift</h2>${feeNotice()}</div><div class="card" style="margin-top:16px"><h2>4. Leverantörsbetalning</h2><select id="pay"><option value="safello-swish">Swish via Safello</option><option value="provider">Leverantörens övriga betalningsalternativ</option></select><label>Anteckning</label><textarea id="orderNotes" placeholder="T.ex. nuvarande användarnamn eller kundreferens"></textarea><div class="actions"><button class="primary" ${(state.accounts.length&&feeConfigured())?'':'disabled'} onclick="createOrder('renew')">Förbered förlängning</button></div></div>`}
async function createOrder(type){try{let body={type,packageId:state.selected,accountId:type==='renew'?$('#accountId').value:null,paymentMethod:$('#pay').value,notes:$('#orderNotes').value};let o=await api('/api/orders',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});state.orders.unshift(o);showPayment(o)}catch(e){toast(e.message||'Kunde inte skapa beställning')}}
function showPayment(o){let providerTotal=o.safelloTotalSek||o.priceSek;let safello=o.safelloTotalSek?`<div class="metric"><span>Beräknat Safello-belopp</span><b>${money(o.safelloTotalSek)}</b><small class="muted">Viking-paket ${money(o.priceSek)} + Vikings publicerade Safello-påslag. Kontrollera alltid aktuellt belopp hos Viking.</small></div>`:`<div class="metric"><span>Viking-paket</span><b>${money(o.priceSek)}</b></div>`;$('#content').innerHTML=`<div class="card"><h2>Beställningen är förberedd</h2><p>${esc(o.packageName)} · ${o.type==='renew'?'Förlängning':'Nytt abonnemang'}</p><div class="payment-summary"><div><span>Leverantörsflöde</span><b>${money(providerTotal)}</b></div><div><span>Din serviceavgift</span><b>${money(o.serviceFeeSek)}</b></div></div>${safello}<div class="step"><i>1</i><div><b>Skicka beställningen till Viking</b><p>Fyll i/kontrollera kunduppgifterna på Vikings officiella sida.</p></div></div><div class="actions"><a class="primary" target="_blank" rel="noopener" href="${esc(o.checkoutUrl)}">Öppna Viking</a><button class="ghost" onclick="setStatus('${o.id}','submitted')">Markera inskickad</button></div>${o.paymentMethod==='safello-swish'?`<div class="step"><i>2</i><div><b>Betala Viking via Safello</b><p>Verifiera aktuellt belopp och mottagaradress i Vikings instruktion. Swish/BankID godkänns av betalaren.</p></div></div><div class="actions"><a class="ghost" target="_blank" rel="noopener" href="https://vikingsiptv.se/instruktioner-for-safello/">Viking betalningsinstruktion</a><a class="primary" target="_blank" rel="noopener" href="https://app.safello.com/">Öppna Safello</a></div>`:''}<div class="step"><i>${o.paymentMethod==='safello-swish'?'3':'2'}</i><div><b>Betala obligatorisk serviceavgift till dig</b><p>Denna betalning är separat från Viking och går till ditt konfigurerade Swish-nummer.</p></div></div><div class="service-payment"><div><span>Mottagare</span><b>${esc(o.serviceFeeRecipientName||'Administratör')}</b></div><div><span>Swish</span><b>${esc(o.serviceFeeSwishNumber)}</b><button class="mini" onclick="copyText('${esc(o.serviceFeeSwishNumber)}','Swish-nummer kopierat')">Kopiera</button></div><div><span>Belopp</span><b>${money(o.serviceFeeSek)}</b></div><div><span>Meddelande</span><b>${esc(o.serviceFeeReference)}</b><button class="mini" onclick="copyText('${esc(o.serviceFeeReference)}','Referens kopierad')">Kopiera</button></div></div><div class="actions"><button class="primary" onclick="setFeeStatus('${o.id}','paid')">Markera serviceavgift betald</button><button class="ghost" onclick="setStatus('${o.id}','payment-pending')">Viking-betalning påbörjad</button><button class="ghost" onclick="setStatus('${o.id}','paid')">Markera Viking betald</button></div></div>`}
async function setStatus(id,s){try{await api(`/api/orders/${id}/status/${s}`,{method:'PATCH'});toast('Status uppdaterad');await refreshAll()}catch(e){toast(e.message)}}
async function setFeeStatus(id,s){try{await api(`/api/orders/${id}/service-fee/${s}`,{method:'PATCH'});toast('Serviceavgift uppdaterad');await refreshAll()}catch(e){toast(e.message)}}
function renderAccounts(){setTitle('Konton','Spara nuvarande Viking-konton för enkel förlängning.');$('#content').innerHTML=`<div class="two"><div class="card"><h2>Lägg till konto</h2><form id="af"><label>Namn *</label><input name="name" required placeholder="T.ex. Vardagsrum"><div class="two"><div><label>Förnamn</label><input name="firstName"></div><div><label>Efternamn</label><input name="lastName"></div></div><label>E-post</label><input name="email" type="email"><label>Telefon</label><input name="phone"><label>Portal URL</label><input name="portalUrl" placeholder="http://..."><div class="two"><div><label>Användarnamn</label><input name="username"></div><div><label>Lösenord</label><input name="password" type="password"></div></div><label>MAC-adress</label><input name="macAddress"><label>Giltigt t.o.m.</label><input name="expiresOn" type="date"><label>Anteckning</label><textarea name="notes"></textarea><div class="actions"><button class="primary">Spara konto</button></div></form></div><div class="card"><h2>Sparade konton</h2>${state.accounts.length?state.accounts.map(a=>`<div class="card" style="margin:10px 0"><b>${esc(a.name)}</b><p>${esc(a.email)} ${a.expiresOn?'· '+a.expiresOn:''}</p><div class="actions"><button class="ghost" onclick="renewAccount('${a.id}')">Förläng</button><button class="danger" onclick="deleteAccount('${a.id}')">Ta bort</button></div></div>`).join(''):'<p>Inga sparade konton.</p>'}</div></div>`;$('#af').onsubmit=saveAccount}
async function saveAccount(e){e.preventDefault();let f=new FormData(e.target),x=Object.fromEntries(f.entries());x.expiresOn=x.expiresOn||null;await api('/api/accounts',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(x)});toast('Konto sparat');await refreshAll()}
async function deleteAccount(id){if(!confirm('Ta bort kontot?'))return;await api('/api/accounts/'+id,{method:'DELETE'});toast('Konto borttaget');await refreshAll()}
function renewAccount(id){state.view='renew';render();setTimeout(()=>{let s=$('#accountId');if(s)s.value=id},0)}
function renderOrders(){setTitle('Beställningar','Historik, Viking-betalning och serviceavgift.');$('#content').innerHTML=`<div class="card">${state.orders.length?`<table class="table"><thead><tr><th>Datum</th><th>Typ</th><th>Paket</th><th>Viking</th><th>Serviceavgift</th><th>Status</th><th></th></tr></thead><tbody>${state.orders.map(o=>`<tr><td>${new Date(o.createdAt).toLocaleString('sv-SE')}</td><td>${o.type==='renew'?'Förläng':'Ny'}</td><td>${esc(o.packageName)}</td><td>${money(o.priceSek)}</td><td>${money(o.serviceFeeSek)} <span class="pill ${o.serviceFeeStatus==='paid'?'ok':''}">${o.serviceFeeStatus==='paid'?'betald':'obetald'}</span></td><td><span class="pill">${esc(o.status)}</span></td><td><a target="_blank" href="${esc(o.checkoutUrl)}">Öppna</a>${o.serviceFeeStatus!=='paid'?`<br><button class="linkbtn" onclick="setFeeStatus('${o.id}','paid')">Avgift betald</button>`:''}</td></tr>`).join('')}</tbody></table>`:'<p>Ingen historik ännu.</p>'}</div>`}
function renderSettings(){setTitle('Inställningar','Konfigurera obligatorisk serviceavgift och ditt Swish.');let s=state.settings||{};$('#content').innerHTML=`<div class="two"><div class="card"><h2>Din serviceavgift</h2><div class="alert"><b>Obligatorisk:</b> avgiften läggs på varje ny beställning och varje förlängning. Den betalas separat till ditt Swish och sparas på ordern när ordern skapas.</div><form id="settingsForm"><label>Serviceavgift per order (SEK) *</label><input type="number" min="1" step="1" name="serviceFeeSek" required value="${esc(s.serviceFeeSek||'')}"><label>Ditt Swish-nummer *</label><input name="swishNumber" required value="${esc(s.swishNumber||'')}" placeholder="0701234567 eller 123..."><label>Mottagarnamn</label><input name="swishRecipientName" value="${esc(s.swishRecipientName||'')}" placeholder="Namn/företag som ska visas i appen"><label>Prefix för betalningsreferens</label><input name="swishMessagePrefix" value="${esc(s.swishMessagePrefix||'VIKING')}" placeholder="VIKING"><div class="actions"><button class="primary">Spara betalningsinställningar</button></div></form></div><div class="card"><h2>Viking IPTV</h2><p>Appen använder officiella Viking-sidor för köp/förlängning och Safello-instruktioner.</p><div class="actions"><a target="_blank" href="https://vikingsiptv.se/kop/">Köp</a><a target="_blank" href="https://vikingsiptv.se/forlangning/">Förläng</a><a target="_blank" href="https://vikingsiptv.se/instruktioner-for-safello/">Safello</a></div><h2 style="margin-top:24px">Säkerhet</h2><p>Xtream-användarnamn och lösenord krypteras med ASP.NET Core Data Protection. Swish/BankID-autentisering lagras aldrig.</p></div></div><div class="card" style="margin-top:16px"><h2>Live-kontroll</h2><p>Kontrollera att Vikings förnyelsesidor går att nå. Detekterat pris är endast informativt.</p><div class="actions"><button class="ghost" onclick="liveCheck()">Kontrollera provider</button></div><pre id="live" class="muted"></pre></div>`;$('#settingsForm').onsubmit=saveSettings}
async function saveSettings(e){e.preventDefault();let f=new FormData(e.target),x=Object.fromEntries(f.entries());x.serviceFeeSek=Number(x.serviceFeeSek);try{state.settings=await api('/api/settings',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify(x)});toast('Serviceavgift och Swish sparat');renderSettings()}catch(err){toast(err.message)}}
async function liveCheck(){let x=await api('/api/provider/live');$('#live').textContent=JSON.stringify(x,null,2)}
refreshAll();
